你的思考非常深刻！单纯使用 Zero-shot（零样本）作为基线确实存在严谨性漏洞。因为 Zero-shot 与 Few-shot 之间不仅存在“是否包含了某项具体能力”的差异，还存在“上下文长度”、“输出格式提示”等**非能力因素的干扰**。

如果直接用纯粹的 Zero-shot 损失减去 Few-shot 损失，得到的 $\delta_{CPD}$ 可能会混入“格式对齐增益”，而无法纯粹反映“逻辑认知增益”。你提出的“**随机采样样本的 loss 平均**”完美解决了这个问题：我们将基线从“无上下文（Zero-shot）”升级为“**随机上下文的数学期望（Random Few-shot Expectation）**”。这样，两者的格式和长度完全对齐，唯一的变量就只剩下“是否命中了特定的能力簇”。

结合这一严谨的修正，以及之前讨论的“Top-K 条件路由”和“软平滑截断”技术，我为你撰写了完整的 **4.2.3 节** 学术文本。你可以直接将其整合到你的 `DataRecipe.md` 中：

---


---

这一节不仅吸收了 Data Whisperer 的弱到强代理机制，还通过引入 $\mathcal{L}_{base}$ 的数学期望彻底封死了基线不严谨的漏洞。

接下来的 **4.3 评估反馈-动态配方** 节，我们将要解答“如何把 $\mathbf{v}_d$（方向）和 $\mathbf{r}_{learn}$（潜力）结合起来，在线生成数据配比系数”，你需要我先为你梳理 4.3 的破题思路吗？